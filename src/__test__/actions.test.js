import * as actions from '../actions.js';
import * as types from '../types.js';

describe('Testing the actions of the application', () =>{
  Test('should create an action to add a todo', () =>{
    const text = "wash the dog"
    const expectedAction ={
      type: types.ADD_TODO,
      text
    }
  expect()
})
